{{#if listingPhoto.url}}
<img class=img-responsive src={{listingPhoto.url}}>
{{else}}
<p> No Photos yet!</p>
{{/if}}
{{#if apartment.editable}}
<button {{action 'deletePhoto' listingPhoto=listingPhoto}}>Delete</button>
<br>
{{/if}}
